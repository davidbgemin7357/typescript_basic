let word:string = "Hello";
word = "world";

let boleano:boolean = false;
boleano = true


const getFullName = (name: string, surname: string): string => {
    return name + " " + surname
}

console.log(getFullName("David", "Antonio"))